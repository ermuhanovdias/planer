# 🔐 Процесс авторизации с верификацией Email

## Как работает авторизация

### 1️⃣ Регистрация нового пользователя

1. Пользователь заполняет форму регистрации (`/register`)
2. Система создает аккаунт в Firebase
3. **Автоматически отправляется письмо с подтверждением email**
4. Пользователь перенаправляется на страницу `/verify-email`

### 2️⃣ Подтверждение Email

На странице `/verify-email` пользователь:
- Видит, что письмо отправлено на его email
- Может переотправить письмо (если не получил)
- После перехода по ссылке в письме нажимает "Я подтвердил email"
- Система проверяет статус верификации
- При успешной верификации → переход на Dashboard

### 3️⃣ Вход в систему

1. Пользователь вводит email и пароль на `/login`
2. **Система проверяет, подтвержден ли email**
3. ❌ Если email НЕ подтвержден → ошибка "Please verify your email"
4. ✅ Если email подтвержден → вход в систему

### 4️⃣ Защита приложения

**Неверифицированные пользователи:**
- ❌ Не могут войти в систему
- ❌ Не имеют доступа к Dashboard
- ❌ Не могут делать API запросы

**Верифицированные пользователи:**
- ✅ Полный доступ к приложению
- ✅ JWT токен автоматически добавляется к запросам
- ✅ Токен обновляется каждые 50 минут

## 🛠️ Технические детали

### Изменения в коде:

**AuthContext.tsx:**
- ✅ При регистрации отправляется `sendEmailVerification()`
- ✅ При входе проверяется `user.emailVerified`
- ✅ В `onAuthStateChanged` пользователь устанавливается только если `emailVerified === true`

**Новые страницы:**
- `/verify-email` - страница с напоминанием о верификации
- Кнопка повторной отправки письма
- Кнопка проверки статуса верификации

**Процесс:**
```
Регистрация → Отправка письма → /verify-email → Переход по ссылке → Проверка → Вход → Dashboard
```

## 📧 Настройка Firebase (важно!)

Для работы верификации email убедитесь, что в Firebase Console включено:

1. **Authentication → Settings → Email enumeration protection** (рекомендуется)

2. **Authentication → Settings → Authorized domains**
   - Добавьте ваш production домен (например, `your-app.pages.dev`)
   - Добавьте `localhost` для разработки
   
3. **Authentication → Templates → Email address verification**
   - Настройте текст письма на русском языке
   
**Важно:** Приложение автоматически настроено на перенаправление:
- После клика по ссылке верификации пользователь возвращается на `https://ваш-домен/verify-email?verified=true`
- Приложение автоматически проверяет статус верификации
- Если email подтвержден → автоматический переход на Dashboard

### Рекомендуемый текст письма:

**Тема:** Подтвердите ваш email

**Текст:**
```
Здравствуйте!

Спасибо за регистрацию в Planer!

Пожалуйста, подтвердите ваш email, нажав на кнопку ниже:

%LINK%

Если вы не регистрировались в нашем сервисе, просто проигнорируйте это письмо.

С уважением,
Команда Planer
```

## 🔄 Что делать, если письмо не пришло?

1. Проверьте папку "Спам"
2. Используйте кнопку "Отправить письмо повторно" на `/verify-email`
3. Убедитесь, что email введен правильно
4. Проверьте, что Email/Password authentication включен в Firebase Console

## 🚀 Запуск в dev режиме

```bash
npm run dev
```

Теперь при регистрации нового пользователя будет обязательна верификация email!

